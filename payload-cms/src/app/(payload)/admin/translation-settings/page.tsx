import { UserTranslationSettings } from '@/components/admin/UserTranslationSettings'

export const metadata = {
  title: 'Translation Settings - Busrom CMS',
}

export default function TranslationSettingsPage() {
  return <UserTranslationSettings />
}
