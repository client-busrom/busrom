# Busrom CMS & Frontend 系统修改与优化汇总 (2026-03-13 ~ 03-16)

这份文档汇总了自 2026 年 3 月 13 日至今的所有关键功能新增、系统改动以及技术修复。

---

## 1. 核心业务功能增强

### 📧 多语言表单自动回复系统
- **24国语言适配**：实现了根据用户提交表单的语言自动匹配回复邮件的功能。支持包括中、英、西、法、德、日、阿（RTL适配）等在内的24种语言。
- **智能称护提取**：邮件正文可自动识别并提取用户在表单填写的“姓名”字段，实现个性化称谓（如：Dear John / 尊敬的 John）。
- **发送者别名管理**：支持在 SMTP 配置中为不同用途的表单设置独立的发送人显示名称。
- **邮件通知机制**：优化了管理接收端配置，支持以逗号/换行符分隔多个管理员接收邮箱。

### 🛒 商城与产品系统优化
- **动态渲染引擎**：开发了全新的产品 Hero 区域渲染逻辑，支持在 CMS 灵活配置产品的展示样式。
- **产品标记系统**：在数据库中新增了“新品 (Is New)”、“热销 (Is Hot)”标签，支持前端筛选和角标显示。
- **商城排序控制**：新增了 `Shop Visibility` 和 `Shop Order` 字段，允许管理员在不删除产品的情况下控制其在商城的可见性及排列顺序。
- **分类管理工具**：集成了 `CategoryProductManager` 组件，管理员可在分类编辑页直接管理该分类下的所有产品排序。

### 📱 全球化交互组件
- **国际化手机输入框 (`PhoneInput`)**：
  - 支持全球国家/地区选择及国旗显示。
  - 自动校验电话号码格式。
  - **地理信息捕获**：提交表单时自动同步用户的 `Country Code`（如 CN）和 `Country Name`（如 China），便于后期精准营销。

---

## 2. CMS 管理后台体验 (UX) 升级

### 🏗️ 菜单架构重组
- **业务逻辑对齐**：根据用户反馈重新组织了侧边栏菜单。将功能划分为：
  - **网站页面管理**（包含首页、子页、商城配置）
  - **产品管理**（包含系列、产品项、模板、规格属性）
  - **内容管理**（包含博客、FAQ、分类、文档模板）
  - **表单管理**（包含配置、提交记录、SMTP设置）
- **状态持久化**：使用 `localStorage` 记住了管理员对菜单的展开/折叠习惯，提升日常操作效率。

### 🌐 翻译中心 (Translation Center)
- **侧边栏集成**：在产品、表单、分类等几乎所有内容集合的侧边栏集成了“翻译中心”，支持 24 种语言内容的快速核对与自动翻译触发。

---

## 3. 技术修复与生产环境稳定性

### 🗄️ 数据库架构同步 (Schema Syncing)
- **RDS 生产环境修复**：手动同步了生产环境数据库中缺失的 20 余个列和表，彻底解决了因 Schema 不一致导致的 500 错误。
- **Locked Documents 修复**：补全了文档锁定机制所需的关联列，防止在多管理员协作时出现锁定报错。
- **关联关系表重建**：修复了 `products_rels`, `categories_rels` 等中间表，确保媒体库关联正常。

### ☁️ 对象存储与 CDN
- **S3/R2 兼容性优化**：修正了 `payload.config.ts` 中的 S3 终端节点逻辑，确保在生产环境下通过 CDN 域正确预览媒体文件。
- **Next.js API 性能**：优化了 `/api/shop/config` 接口的聚合逻辑，减少了列表页的首屏加载时间。

---

## 4. 涉及的关键文件清单 (Git Stats)

### Payload (CMS 后端):
- `payload.config.ts` (主配置 & S3/审计日志)
- `src/collections/` (Products, ProductSeries, FormConfigs, SmtpConfigs 等)
- `src/components/admin/` (CustomNav, GlobalAdminBanner)
- `src/lib/form-email.ts` (邮件逻辑)
- `scripts/` (多份生产环境修复脚本 v3~v6)

### Web (前端):
- `web/components/ui/PhoneInput.tsx`
- `web/app/[locale]/shop/[slug]/ProductDetailClient.tsx`
- `web/app/api/products/route.ts`
- `web/lib/types/product.ts`

---
**核对结论**：本次工作循环已基本清除 3 月 13 日之前的技术债，并为后续的商城运营奠定了坚实的功能基础。
