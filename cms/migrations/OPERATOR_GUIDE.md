# 媒体系统使用指南（运营人员版）

**Media System User Guide for Operators**

---

## 🎯 使用场景

当 MediaCategory、MediaTag、Media 都为空时，运营人员需要按以下步骤操作：

---

## 📋 操作流程（推荐）

### 方案 A：使用预设数据（推荐，5分钟完成）⭐

**适用场景**：快速搭建标准分类系统

#### Step 1: 运行预设数据

**方法**：使用 Keystone 的 `onConnect` hook 自动运行种子数据

📖 **详细教程**：参见 [README_SEEDS.md](./README_SEEDS.md)

**快速步骤**：

1. **系统已自动配置**！`cms/keystone.ts` 已包含自动初始化代码：

```typescript
import { seedMediaSystem } from './migrations/seed-media-system'

db: {
  provider: 'postgresql',
  url: process.env.DATABASE_URL!,
  // ... 其他配置

  // 添加这个 hook
  async onConnect(context) {
    const categoryCount = await context.query.MediaCategory.count()
    if (categoryCount === 0) {
      console.log('🌱 Seeding media system...')
      await seedMediaSystem(context)
      console.log('✅ Seed completed!')
    }
  },
},
```

2. **启动 Keystone**：

```bash
cd cms
npm run dev
```

3. **等待自动完成**（约 5-10 秒）

**执行结果**：
- ✅ 自动创建 8 个媒体分类（树形结构）
- ✅ 自动创建 25 个标签（产品系列、功能类型、场景类型、规格、颜色）

#### Step 2: 验证预设数据

访问 Keystone Admin UI（http://localhost:3000）：

1. 点击 **MediaCategory**
   - 应该看到 8 个分类
   - 检查层级关系（Common、Product、By Product Series、By Function 等）

2. 点击 **MediaTag**
   - 应该看到 25 个标签
   - 检查标签类型（按颜色区分）

#### Step 3: 上传图片

1. 点击 **Media**
2. 点击 **Create Media**
3. 填写表单：
   - **File**: 上传图片（会自动上传到 AWS S3）
   - **Filename**: `glass-standoff-scene-01.jpg`
   - **Status**: ACTIVE
   - **Alt Text**: 点击编辑，输入多语言描述
     - 英文：`Glass Standoff in modern bathroom scene`
     - 中文：`广告螺丝在现代浴室场景中的应用`
   - **Primary Category**: 选择 `Product > By Function > Scene Photo`
   - **Tags**: 选择多个标签
     - `Glass Standoff`（产品系列）
     - `Scene Photo`（功能类型）
     - `Single Scene`（场景类型）
     - `50mm`（规格）
     - `Black`（颜色）
   - **Metadata**: 填写额外信息（JSON 格式）
     ```json
     {
       "sceneNumber": 1,
       "sceneType": "单独",
       "seriesNumber": 2,
       "specs": ["50mm", "不锈钢"],
       "colors": ["黑色"]
     }
     ```
4. 点击 **Save**

✅ 完成！现在可以继续上传更多图片。

---

### 方案 B：手动创建分类和标签（灵活，30分钟完成）

**适用场景**：需要自定义分类结构

#### Step 1: 创建顶级分类

1. 访问 http://localhost:3000
2. 点击 **MediaCategory** → **Create MediaCategory**
3. 创建第一个顶级分类：

   **示例 1：通用分类**
   - **Name**:
     - 点击 JSON 编辑器
     - 英文：`Common`
     - 中文：`通用`
   - **Slug**: `common`
   - **Parent**: （留空）
   - **Depth**: 0（自动计算）
   - **Type**: COMMON
   - **Order**: 1
   - **Icon**: `folder`
   - **Description**:
     - 英文：`Common media files not specific to products`
     - 中文：`通用媒体文件，不特定于产品`
   - 点击 **Save**

4. 创建第二个顶级分类：

   **示例 2：产品分类**
   - **Name**: `{"en": "Product", "zh": "产品"}`
   - **Slug**: `product`
   - **Parent**: （留空）
   - **Type**: PRODUCT_SERIES
   - **Order**: 2
   - **Icon**: `package`
   - 点击 **Save**

#### Step 2: 创建子分类

1. 在 **Product** 分类下创建子分类：

   **示例：按功能分类**
   - **Name**: `{"en": "By Function", "zh": "按功能分类"}`
   - **Slug**: `product-by-function`
   - **Parent**: 选择 `Product`
   - **Depth**: 1（自动计算）
   - **Type**: PRODUCT_FUNCTION
   - **Order**: 1
   - 点击 **Save**

2. 在 **By Function** 下创建第三层分类：

   **示例：场景图**
   - **Name**: `{"en": "Scene Photo", "zh": "场景图"}`
   - **Slug**: `scene-photo`
   - **Parent**: 选择 `By Function`
   - **Depth**: 2（自动计算）
   - **Type**: PRODUCT_FUNCTION
   - **Order**: 1
   - **Icon**: `image`
   - 点击 **Save**

⚠️ **重要**：最多只能创建 3 层分类（depth: 0, 1, 2）

#### Step 3: 创建标签

1. 点击 **MediaTag** → **Create MediaTag**
2. 创建产品系列标签：

   **示例 1：Glass Standoff**
   - **Name**: `{"en": "Glass Standoff", "zh": "广告螺丝"}`
   - **Slug**: `glass-standoff`
   - **Type**: PRODUCT_SERIES
   - **Color**: `#3b82f6`（蓝色）
   - **Order**: 1
   - 点击 **Save**

3. 创建功能类型标签：

   **示例 2：Scene Photo**
   - **Name**: `{"en": "Scene Photo", "zh": "场景图"}`
   - **Slug**: `func-scene-photo`
   - **Type**: FUNCTION_TYPE
   - **Color**: `#22c55e`（绿色）
   - **Order**: 1
   - 点击 **Save**

4. 重复以上步骤，创建更多标签

**建议创建的标签类型**：
- PRODUCT_SERIES（产品系列）：10-20 个
- FUNCTION_TYPE（功能类型）：5 个
- SCENE_TYPE（场景类型）：4 个
- SPEC（规格）：按需创建
- COLOR（颜色）：按需创建

#### Step 4: 上传图片

（同方案 A 的 Step 3）

---

## 📊 推荐的分类结构

### 标准三层结构

```
📁 Common（通用）                          [depth: 0]

📁 Product（产品）                         [depth: 0]
   ├─ 📁 By Product Series（按产品系列）   [depth: 1]
   │  └─ （未来可添加具体系列子分类）
   │
   └─ 📁 By Function（按功能分类）         [depth: 1]
      ├─ 📄 Scene Photo（场景图）          [depth: 2]
      ├─ 📄 White Background（白底图）     [depth: 2]
      ├─ 📄 Dimension Drawing（尺寸图）    [depth: 2]
      ├─ 📄 Real Shot（实拍图）            [depth: 2]
      └─ 📄 Composite Use（合用图）        [depth: 2]
```

### 推荐的标签分组

#### 1. 产品系列标签（PRODUCT_SERIES）

| 英文名称 | 中文名称 | Slug | 颜色 |
|---------|---------|------|------|
| Glass Standoff | 广告螺丝 | glass-standoff | #3b82f6 |
| Glass Connected Fitting | 玻璃栏杆扶手连接件 | glass-connected-fitting | #8b5cf6 |
| Glass Fence Spigot | 玻璃护栏支架底座 | glass-fence-spigot | #ec4899 |
| Guardrail Glass Clip | 护栏系列 | guardrail-glass-clip | #f59e0b |
| Bathroom Glass Clip | 浴室系列 | bathroom-glass-clip | #10b981 |
| Glass Hinge | 浴室夹 | glass-hinge | #06b6d4 |
| Sliding Door Kit | 移门滑轮套装 | sliding-door-kit | #6366f1 |
| Bathroom Handle | 浴室&大门拉手 | bathroom-handle | #14b8a6 |
| Door Handle | 大门拉手 | door-handle | #f97316 |
| Hidden Hook | 挂钩 | hidden-hook | #84cc16 |

#### 2. 功能类型标签（FUNCTION_TYPE）

| 英文名称 | 中文名称 | 颜色 |
|---------|---------|------|
| Scene Photo | 场景图 | #22c55e |
| White Background | 白底图 | #a3e635 |
| Dimension Drawing | 尺寸图 | #facc15 |
| Real Shot | 实拍图 | #fb923c |
| Composite Use | 合用图 | #f87171 |

#### 3. 场景类型标签（SCENE_TYPE）

| 英文名称 | 中文名称 | 颜色 |
|---------|---------|------|
| Normal Scene | 普通场景图 | #86efac |
| Single Scene | 单独场景图 | #86efac |
| Combination Scene | 场景组合图 | #86efac |
| Series Scene | 系列场景图 | #86efac |

#### 4. 规格标签（SPEC）

根据实际产品规格创建，例如：
- `50mm`, `100mm`, `150mm`（长度）
- `Stainless Steel`, `Brass`（材质）
- 颜色：`#fbbf24`（金色）

#### 5. 颜色标签（COLOR）

根据实际产品颜色创建，例如：
- `Black（黑色）`, `Silver（银色）`, `Gold（金色）`
- 标签颜色：`#9ca3af`（灰色）

---

## 🎨 图片上传最佳实践

### 1. 文件命名规范

**推荐格式**：`{系列}-{功能}-{场景编号}.{扩展名}`

**示例**：
- `glass-standoff-scene-01.jpg`（广告螺丝-场景图-01）
- `glass-hinge-white-bg-01.jpg`（浴室夹-白底图-01）
- `door-handle-dimension-01.jpg`（门拉手-尺寸图-01）

### 2. Alt Text 填写规范

**必填语言**：英文、中文

**格式**：`{产品名称} + {场景描述}`

**示例**：
- 英文：`Glass Standoff in modern bathroom scene`
- 中文：`广告螺丝在现代浴室场景中的应用`

### 3. Primary Category 选择规则

**原则**：选择最具体的分类

**示例**：
- ✅ 推荐：`Product > By Function > Scene Photo`
- ❌ 不推荐：`Product`（太泛）

### 4. Tags 选择规则

**建议组合**：至少选择 2-3 个标签

**示例组合**：
- **产品系列标签**：`Glass Standoff`（必选）
- **功能类型标签**：`Scene Photo`（必选）
- **场景类型标签**：`Single Scene`（可选）
- **规格标签**：`50mm`（可选）
- **颜色标签**：`Black`（可选）

### 5. Metadata 填写规范

**常用字段**：

```json
{
  "sceneNumber": 1,              // 场景编号（数字）
  "sceneType": "单独",            // 场景类型（单独/组合/系列）
  "seriesNumber": 2,             // 系列编号（数字）
  "specs": ["50mm", "不锈钢"],    // 规格列表
  "colors": ["黑色", "银色"],     // 颜色列表
  "photographer": "张三",         // 摄影师（可选）
  "shootDate": "2024-01-15"      // 拍摄日期（可选）
}
```

---

## 🔍 常见问题

### Q1: 为什么 depth 字段是只读的？
**A**: depth 会根据父分类自动计算，无需手动填写。

### Q2: 创建第 4 层分类时报错怎么办？
**A**: 系统限制最多 3 层分类（depth: 0, 1, 2）。请调整分类结构。

### Q3: 如何修改已上传的图片？
**A**: 点击 Media 列表中的图片，修改后点击 Save。注意：不能删除图片（只能改为 ARCHIVED 状态）。

### Q4: 如何归档不需要的图片？
**A**: 将 Status 改为 ARCHIVED（图片不会被删除，只是隐藏）。

### Q5: 如何批量上传图片？
**A**: 当前版本不支持批量上传。需要逐个上传。

### Q6: JSON 字段怎么填写？
**A**: 点击字段旁边的编辑按钮，会打开专用的多语言编辑器。

### Q7: 标签颜色有什么作用？
**A**: 用于 UI 显示，方便在列表中快速区分标签类型。

### Q8: mediaCount 字段显示什么？
**A**: 自动显示该分类/标签下有多少张图片，无需手动维护。

---

## 🚀 快速上手示例

### 场景：上传第一张产品场景图

1. **访问 CMS**: http://localhost:3000
2. **确保已有分类和标签**（使用方案 A 预设数据）
3. **点击 Media → Create Media**
4. **填写表单**：
   - File: 上传 `glass-standoff-scene-01.jpg`
   - Filename: `glass-standoff-scene-01.jpg`
   - Status: ACTIVE
   - Alt Text:
     - en: `Glass Standoff mounted on modern glass door`
     - zh: `广告螺丝安装在现代玻璃门上`
   - Primary Category: `Product > By Function > Scene Photo`
   - Tags:
     - `Glass Standoff`
     - `Scene Photo`
     - `Single Scene`
     - `50mm`
   - Metadata:
     ```json
     {
       "sceneNumber": 1,
       "sceneType": "单独",
       "specs": ["50mm"],
       "colors": ["银色"]
     }
     ```
5. **点击 Save**

---

## 📞 需要帮助？

- **技术问题**：联系开发团队
- **数据导入**：使用预设数据脚本（见方案 A）
- **自定义需求**：参考 `cms/migrations/seed-media-system.ts`

---

**最后更新**: 2025-11-02
